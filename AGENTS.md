## 开发规范
- 这是一个二开项目，需要持续的合并源仓库，所有改动要考虑以后合并源仓库减少冲突
- 所有 dj.svg 的地方要替换成 icon.webp
- 后续新增、恢复或调整任何二开功能、品牌替换、发布流程、默认配置变化，都必须同步登记到本文件，避免后续合并上游时二开功能被覆盖

## 二开功能记录与维护要求
- 本清单按作者 `iamobj` 的非 merge commit 整理；合并上游的 merge commit 只作为同步背景，不单独视为二开功能
- 合并上游后如果出现同类文件冲突，以本清单记录的二开业务行为为准，尽量小范围恢复，减少长期冲突面
- README 等纯文档更新不拆成独立二开功能；如果文档描述涉及二开能力或发布流程，也要同步维护本清单

### iamobj 二开清单
- 品牌与默认文案去上游化（`deddde7`、`2d2a22f`）：移除 `Dujiao-Next` 默认品牌、GitHub 链接、控制台品牌输出；默认站点名为空或使用本项目默认值，避免重新露出上游品牌
- 图标替换（`48d1458`、`f051bee`）：删除 `public/dj.svg`，统一使用 `public/icon.webp`；默认 favicon 回落到 `/icon.webp`
- 联系方式展示（`9199947`、`eba5e4a`、`e1f2dee`）：页脚 WhatsApp 配置按微信联系方式展示，并保留微信样式入口；底部品牌区域展示站点 logo 或本项目默认图标
- 订单流交付标签（`1dfd906`、`e147c16`）：商品或订单项包含 `订单流交付` 标签时，前台交付类型显示为自动交付，并隐藏该内部标签；购物车、结算、商品详情、订单详情都要保留此行为
- 注册流程（`4681883`）：注册页不展示、不强制勾选隐私/服务协议；提交注册时固定传 `agreement_accepted: true`
- 本地开发代理（`25b5429`、`2d2a22f`）：Vite dev server 保持 `5173`，`/api` 和 `/uploads` 代理到 `http://localhost:38080`；保留分销商子域名 Host 解析相关配置
- 发布与镜像（`869a46d`、`6d589d3`、`77f7c86`）：保留 `npm/pnpm run release:tag` 对应的 `scripts/release-tag.mjs`；tag 推送触发 GHCR 镜像发布，并构建 `linux/amd64` 和 `linux/arm64`
